# 240-bear-studio ｜ 3Dくまさんスタジオ

## 0. ドキュメント情報

- 仕様書名: 3Dくまさんスタジオ
- 対象ゲーム: `240-bear-studio`（旧 `240-day064`）
- 作成日: 2026-09-16
- 更新日: 2026-09-16
- ステータス: 実装済み（harness PASS。iPhone 実機は未検証）
- 参照ファイル: `index.html`（単一ファイル）

## 批評（コントロールパネル実装前）

- これは**ゲームではなく操作トイ**。勝敗も目的も無い。無理にスコアを付けず「触って眺める」ものとして仕上げる。
- `<title>` が英語のみ、`<h1>` が「3D 3Dくまさんスタジオ」と3Dが重複していた。
- 毛色スウォッチが 28px（`w-7 h-7`）で、指では押せない大きさだった。
- **アニメーションを止められなかった**。眺めるためのアプリなのに、ポーズを見て止めることができない。
- 毛色・モーション・リボンを選び直しても、開き直すと毎回ブラウンの待機に戻る。設定が残らない。
- 外部CDN 4本（Tailwind / three.js / OrbitControls / FontAwesome）＋ Google Fonts に依存していた。**外部18リクエスト・canvas出現まで8.8秒**で、モバイル回線では確実に破綻する。今回すべて同梱・置換して解消した。

## 1. 概要

- ジャンル: 3D ビューア／着せ替えトイ
- 一言説明: 3Dのくまを6つのモーションで動かし、毛色とリボンを着せ替える。
- 想定プレイ時間: 1〜3分（気が向いたときに開く）
- 想定プレイヤー: iPhone 片手。子どもと一緒に触る用途も想定

## 2. 対象環境

### 必須
- 配信先: GitHub Pages
- 最優先端末: iPhone Safari
- 描画: Three.js r128（WebGL）＋ OrbitControls。**どちらも同梱**（`three.min.js` / `OrbitControls.js`）
- CSS: `style.css`（Tailwind CDN の置き換え。使うユーティリティだけを手書き）
- アイコン: 絵文字（FontAwesome 廃止）／フォント: システムフォント（Google Fonts 廃止）
- **外部リクエスト 0件**。公開実体 0.64MB

### 任意
- PC: ドラッグ=回転 / 右ドラッグ=移動 / ホイール=ズーム / P・Space=一時停止 / R=視点リセット

## 3. ファイル構成

```text
240-bear-studio/
  index.html
  style.css           # Tailwind CDN の置き換え
  three.min.js        # r128（同梱）
  OrbitControls.js    # r128 examples（同梱）
  SPEC.md
  LEARNINGS.md
```

## 4. 操作／設定UI（コントロールパネル）

| 項目 | 実装 |
|---|---|
| 入力方式（キー） | **P / Space＝一時停止・再生**、**R＝視点リセット** |
| 入力方式（タッチ） | OrbitControls（1本指=回転 / 2本指=移動・ズーム） |
| touch-action | body は `manipulation`、`#canvas-container` と canvas は `none` の2段構え |
| 一時停止 | ヘッダー右の再生/停止ボタン。`deltaTime` を0にして**ポーズを崩さずその場で止める** |
| 自動停止 | `visibilitychange` でタブを離れたら自動停止（iPhoneの発熱・電池対策） |
| 視点リセット | ヘッダー右の照準ボタン（R キー）。**画面のアスペクト比から全身が入る距離を計算して戻す** |
| 縦持ち対応 | 起動時とリサイズ時に自動フィット。ただし**一度でも自分で視点を動かしたら以後は勝手に戻さない**（`controls` の `start` でロック） |
| ミュート | **置いていない。この作品には音源が1つも無い**ため（鳴らないものは消せない） |
| 設定の保存 | `tg.240.anim`（モーション） / `tg.240.preset`（毛色） / `tg.240.bowtie`（リボン）。次に開くと前回の姿で始まる |
| タップ領域 | システムボタン・毛色スウォッチともに 44px 以上。選択中は `aria-pressed="true"` |

- モーションを選ぶと一時停止は自動で解除する（止めたまま選んで「壊れた」と誤解させないため）

## 5. 中身

### モーション6種
待機 / トコトコ歩く / 手を振る / 大ジャンプ / ノリノリダンス / すやすや眠る

### 毛色4種
ブラウン / シロクマ / パンダ（手足だけ黒）/ ピンク

### リボン
オン・オフのトグル。状態は保存される。

## 6. 未確定事項

- iPhone 実機での発熱とフレームレート（未検証）
- 絵文字アイコンの見え方は OS により異なる（iOS / Android / Windows で字形が変わる）。文字ラベルを併記しているので意味は通る想定
- `controls.maxDistance` が 15 のため、縦持ちでは自動フィット距離が上限に張り付いている。全身は収まっているが、さらに細い画面では窮屈になる可能性がある
