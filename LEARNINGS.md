# 240-bear-studio LEARNINGS

## 2026-09-16 コントロールパネル実装＋タイトル付与

- フォルダ名を `240-day064` から `240-bear-studio` へ、エントリを `3d_bear_model_animator.html` から `index.html` へ変更。ファイル名が index.html でないと GitHub Pages でフォルダURLから開けない。
- `<title>` は英語のみ、`<h1>` は「3D 3Dくまさんスタジオ」と3Dが重複していた。両方「3Dくまさんスタジオ」に統一。
- **音源が1つも無い作品にミュートは置かなかった**。共通方針の「ミュートを必ず置く」は「鳴るのに消せない47本」への対策であって、無音の作品にダミーのトグルを足す話ではない。SPECに理由を明記した。
- 代わりにこの作品の「止めたい」は**アニメーションの一時停止**だった。`deltaTime` を0にする方式にすると、ポーズを崩さずその場で固まるので、見たい姿勢で止められる。`clock.getDelta()` を再開時に1回捨てないと溜まった時間で瞬間移動する。
- 毛色スウォッチが 28px（Tailwind の `w-7 h-7`）だった。44px へ拡大。Tailwindのユーティリティを直接並べると、こういう寸法が指のサイズと無関係に決まってしまう。
- 設定は `tg.240.anim` / `tg.240.preset` / `tg.240.bowtie` で保存。次に開くと前回の姿で始まる。
- **描画コスト**: `antialias:true` ＋ `PCFSoftShadowMap` ＋ `shadow.mapSize 2048` ＋ `pixelRatio 2` で、ソフトウェア描画環境では 11 RAF/秒まで落ちていた。`antialias:false` / `PCFShadowMap` / `mapSize 512` / `pixelRatio 1.5` へ変更して回復。塗り面積が支配的で、ポリゴン数は効かない。
- **計測の教訓**: 同一コードで RAF が 35 → 7 まで振れた。原因は機械側の負荷（別アプリが常時100%消費＋連続WebGL計測による発熱）だった。**数値が下がったからとコードを削るのは誤り**で、まず計測環境を疑う。
- **外部CDN 4本＋Google Fonts を全部外した**。きっかけは harness の `Canvas: なし`。計測したら **外部18リクエスト・canvas出現まで8.8秒**だった。内訳で一番効いていたのは Google Fonts で、Noto Sans JP のサブセットだけで10本以上取りに行っていた。
  - `window.onload` は**全サブリソースを待つ**。フォント10本が終わるまで `initScene()` が始まらない構造だった。`DOMContentLoaded` に変えた
  - Tailwind CDN は実行時コンパイラで、本番用ではない。使っていたユーティリティは約70個だけだったので `style.css` に手書きで写した
  - FontAwesome はアイコン13個のためにCSS＋webfontを取りに行っていた。絵文字に置換（文字ラベルが併記されているので意味は落ちない）
  - 結果: **外部リクエスト 18件 → 0件**、networkidle 3158ms → 1077ms、公開実体 0.64MB
- **iPhone縦持ちでくまが見切れていた**。`fov 45` 固定＋カメラ距離固定なので、アスペクト比 0.46 では横視野が極端に狭くなる。縦横それぞれの視野から必要距離を計算して合わせる `fitFraming()` を入れた。**主戦場の端末で確認するまで気づけない類のバグ**（スクリーンショットを撮って初めて分かった）
  - ただし自動フィットは「起動時・リサイズ時・視点リセット時」だけにした。ユーザーがピンチで寄せた後に勝手に戻ると操作を奪う
- 検証: harness PASS（43 RAF・タップ成功）／操作盤の機能テスト **25/25 PASS**（外部リクエスト0件、全身が画面内に収まる、設定3種の保存と復元を含む）
- 未検証: iPhone 実機の発熱・FPS。絵文字アイコンのOS別の見え方。

## 2026-09-19 旧エントリURLの404を修復
- 症状: `https://titan11111.github.io/240-bear-studio/3d_bear_model_animator.html` が **404**。本体（`/240-bear-studio/`）は 200 で生きていた
- 原因: エントリを `3d_bear_model_animator.html` → `index.html` へ改名したため、**改名前に配ったリンクだけが死んだ**
- 対処: `3d_bear_model_animator.html` を index.html へのリダイレクト専用ページとして復活（meta refresh ＋ `location.replace()`。`?query`・`#hash` も引き継ぐ）
- 検出元: `_tools/check-legacy-entry.sh`（245の同種事故を機に新設）。本番URLへcurlを撃って検出
- 鉄則8: エントリ名を変えたら旧名をリダイレクトで必ず残す（本体URLが200のままなので気づけない）
